<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách Tour của Partner</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <style>
        body {
            padding: 20px;
        }
        table {
            width: 100%;
            margin-top: 20px;
        }
        th, td {
            text-align: center;
        }
    </style>
</head>
<body>
    <%- include('../header', { pageTitle: ' ' }) %>
    <div class="container">
        <h1 class="mb-4">Danh sách Tour của Partner</h1>
        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Tiêu đề</th>
                    <th>Mô tả</th>
                    <th>Giá</th>
                    <th>Địa điểm</th>
                    <th>Thời gian</th>
                    <th>Trạng thái</th>
                    <th>Partner</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                <% if (tours.length === 0) { %>
                    <tr>
                        <td colspan="8">Không có tour nào.</td>
                    </tr>
                <% } else { %>
                    <% tours.forEach(tour => { %>
                        <tr>
                            <td><%= tour.title %></td>
                            <td><%= tour.description %></td>
                            <td><%= tour.price %> VNĐ</td>
                            <td><%= tour.location %></td>
                            <td><%= tour.duration %> ngày</td>
                            <td><%= tour.isDisabled ? 'Tắt' : 'Bật' %></td>
                            <td><%= tour.partner ? tour.partner._id : 'Không có' %></td> 
                            <td>

                                <form action="/admin/tours/toggle-status/<%= tour._id %>" method="POST" style="display:inline;">
                                    <button type="submit" class="btn btn-secondary btn-sm"><%= tour.isDisabled ? 'Bật' : 'Tắt' %></button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
