<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Delete Tour Detail</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .form-delete-tour-detail {
      max-width: 600px;
      margin: 20px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .form-delete-tour-detail fieldset {
      border: none;
      padding: 0;
    }
    .form-delete-tour-detail legend {
      font-size: 1.5em;
      margin-bottom: 10px;
    }
    .input-group {
      margin-bottom: 15px;
    }
    .input-group label {
      display: block;
      margin-bottom: 5px;
      font-weight: bold;
    }
    .input-group input {
      width: 100%;
      padding: 8px;
      border: 1px solid #ddd;
      border-radius: 4px;
    }
    .btn {
      background-color: #007bff;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    .btn-danger {
      background-color: #dc3545;
    }
    .btn-danger:hover {
      background-color: #c82333;
    }
  </style>
</head>
<body>
  <%- include('../header', { pageTitle: 'Delete Tour Detail' }) %>

  <div class="form-delete-tour-detail">
    <form action="/delete_tour_detail" method="post">
      <fieldset>
        <legend>Delete Tour Detail</legend>

        <!-- Hidden field to store the TourDetail ID -->
        <div class="input-group">
          <label for="tourDetailId">Tour Detail ID:</label>
          <input type="text" id="tourDetailId" name="tourDetailId" value="<%= tourDetailEdit._id %>" readonly />
        </div>

        <div class="input-group">
          <label>Are you sure you want to delete this tour detail?</label>
        </div>

        <div>
          <button type="submit" class="btn btn-danger">Confirm Delete</button>
        </div>
      </fieldset>
    </form>
  </div>
</body>
</html>
